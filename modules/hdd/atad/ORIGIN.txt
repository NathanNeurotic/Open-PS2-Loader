Vendored from: ps2dev/ps2sdk, iop/dev9/atad (built with the iop/dev9/ata_bd flag set:
ATA_USE_DEV9=1 ATA_GAMESTAR_WORKAROUND=1 ATA_ENABLE_BDM=1)
Pin: 426de26cc7fe6d3d0e33d5704fa6815061ee9b27 (2026-07-20)
License: AFL 2.0 (headers retained in the sources)

WHY VENDORED (2026-07-21, maintainer HW report "40x: HardDisk Drive not detected" persisting
through the #249 retry loop): the stock driver latches ata_devinfo_init=1 BEFORE probing, and
_start() probes at module load -- a drive still spinning up at that instant stays "dead" for the
whole session, and every later sceAtaInit (OPL's EE retry, xhdd's re-probe devctl) is a no-op
returning the corpse. No EE-side code can heal a latched IOP driver; the fix must live here.

LOCAL CHANGES vs the pin (all marked /* FORK */ in src/ps2atad.c):
1. sceAtaInit latches init-done only on SUCCESS -> failed probes are retryable.
2. ata_init_devices returns ATA_RES_ERR_NODEV when device 0 answered the probe but failed
   IDENTIFY (the spin-up race) -- previously it returned 0 and latched a dead devinfo.
3. g_ata_bd_connected[] guard: bdm_connect_bd at most once per unit across re-probes.
4. The g_ata_bd[i].path assignment is DROPPED: the field was appended to struct block_device on
   2026-02-26 and is absent from the PS2MAXSDK container's bdm.h; it is additive (all prior
   offsets identical -> ABI-safe to omit), bdmfs_fatfs always registers "mass" separately, and
   the fork forces massN: paths anyway (repo doctrine).
5. Makefile is fork-authored (exports.o-first ordering; see the comment in it).
6. ata_bd_write refuses a BDM write below the first APA user partition (LBA < 0x40000) when LBA 0
   carries the APA magic, or cannot be read to prove it does not (2026-09, docs/APA-SAFETY.md).
   The partition table, error records and journal live there; no FAT/exFAT volume ever does.
7. Backported upstream f30f05eba (2026-08-27): ata_device_sector_io64 counts progress in a u32
   chunk, so a 65536-sector LBA48 request no longer re-issues the same command forever.
8. ata_device_sector_io64 refuses a request outside the drive's addressable range before issuing
   any command (ata_request_in_range): on a drive without LBA48 the 28-bit taskfile silently wrapped
   a sector at or above 2^28 to the start of the disk, and nothing checked capacity. The u32
   total_sectors_lba48 saturates past 2 TiB, so such drives are bounded by the 48-bit width instead.
9. A drive whose LOGICAL sectors are not 512 bytes (4Kn; IDENTIFY word 106 bit 12, size in words
   117-118) is refused all sector I/O in ata_device_sector_io64, and its BDM device reports that size
   so BDM declines it (code 500 in the loader). 512e drives (4K physical, 512 logical) are unaffected.
Changes 6-9 are pinned by .github/scripts/test_apa_table_safety.py.

Also picked up from the pin (missing from BOTH pinned build containers): upstream ab3c5a883
("fix device probing", per-device SoftReset+select loop) and 221f6e27a (DVRP device-selection
workaround).

RE-VENDOR: re-fetch src/ps2atad.c, src/imports.lst, src/irx_imports.h, src/exports.tab from the
new pin, re-apply changes 1-4, 6, 8 and 9 (7 is upstream from f30f05eba on), update this file's pin line, `rm -rf obj/` before rebuilding.
NOT vendored: include/atad.h (the installed SDK's copy is used so xhdd and this module compile
against the same header per container; stable since 2024-12).
HDPro: hdpro_atad.irx remains the SDK prebuilt (its own latch unfixed; vendor separately only if
an HDPro rig ever reports this symptom).
